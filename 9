class Solution {
    fun isPalindrome(x: Int): Boolean {
        val tString = x.toString()
        val reverse = tString.reversed()
        
        if(tString == reverse)
        	return true
        else
        	return false
    }
}
